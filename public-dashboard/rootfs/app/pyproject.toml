[project]
name = "public-dashboard"
version = "0.0.9"
description = "A secure, dynamic dashboard for building systems with admin entity management"
requires-python = "==3.14.*"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "httpx",
    "pydantic",
    "python-multipart",
    "pyyaml",
    "python-dotenv",
    "pyjwt",
]
