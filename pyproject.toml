[project]
name = "hadash"
version = "0.0.4"
description = "Home Assistant Public Dashboard Add-on Repository"
requires-python = "==3.14.*"

[dependency-groups]
dev = [
    "pre-commit-uv",
    "ruff",
]

[tool.uv.workspace]
members = ["public-dashboard/rootfs/app"]
